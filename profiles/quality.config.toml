model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = """
Use the installed omnicodex routing skill with the Quality policy. Keep Sol High as the control plane, use Terra and Sol roles for bounded work and independent review, and reserve Astra for a concrete hard blocker or critical decision.
"""

[agents]
enabled = true
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "medium"
max_concurrent_threads_per_session = 6
