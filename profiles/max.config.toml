model = "gpt-6-astra"
model_reasoning_effort = "high"
developer_instructions = """
Use the installed omnicodex routing skill with the Max policy. Keep Astra High as the control plane while delegating deterministic or bounded phases to the cheapest capable installed role when doing so preserves quality.
"""

[agents]
enabled = true
default_subagent_model = "gpt-5.6-sol"
default_subagent_reasoning_effort = "medium"
max_concurrent_threads_per_session = 6
